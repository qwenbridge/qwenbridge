# V8.1 Release Evidence

## Commands

```bash
mvn -B -Punit-tests test
mvn -B -Pintegration-tests test
bash scripts/verify-v8.1-operability.sh
bash scripts/verify-release.sh
docker compose --profile production config
```

## Acceptance criteria

- Health contracts are stable.
- Dependency degradation returns sanitized reasons.
- Metrics names use the documented `qwenbridge.` convention.
- Production profile validates required runtime configuration.
- Release tag target is `v0.8.1`.
