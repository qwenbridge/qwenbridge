spring:
  application:
    name: omnisearch-ai

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

omnisearch:
  ollama:
    base-url: http://localhost:11434
    qwen-model: qwen2.5
    embedding-model: bge-m3
